# 项目范围

第一阶段重点是核心本地检索：配置 OpenViking、导入 markdown 语料、执行语义检索，并用项目 CLI 输出结构化结果。

第二阶段新增轻量检索型调研 workflow：读取 YAML 问题列表，多次检索 OpenViking，并生成带 `viking://` 引用的 markdown 草稿。

当前仍然不要求 VikingBot、AnySearch、Web UI，也不要求导入大型 GitHub 仓库。完整 LLM 报告生成放在后续阶段。
