interface:
  display_name: "AnyViking Research"
  short_description: "Search, index, retrieve, and draft research with AnySearch plus OpenViking."
  default_prompt: "Use $anyviking-research to search sources, index them with OpenViking, and produce a retrieval-backed research draft."

policy:
  allow_implicit_invocation: true
